# Chinese Output Templates

Loaded when the review is rendered in Chinese (中文). SKILL.md is authoritative for review logic and the Localization Rule; these templates are the concrete Chinese output shapes.

#### Chinese Default Developer Review

```markdown
# 提交前审查

**VERDICT:** <SAFE_TO_COMMIT | SAFE_TO_COMMIT_WITH_NOTES | DO_NOT_COMMIT>
**结论：** <简短、可执行的 1-2 句决策摘要：是否可提交 + 主要依据/最高风险 + 提交前必须动作或建议验证>
**统计：** <N 个阻塞项 · N 个非阻塞提醒 · N 个测试缺口 · N 个审查限制；四项均为 0 时写 无>
**差异来源：** <来源 + 精确范围：命令/PR/commit range/用户粘贴 diff；说明 staged/unstaged/基准分支；未知时写明未知>
**审查范围：** <若 diff 的每个 hunk 都已审查则为完整审查；仅当 diff 本身有无法审查的部分（截断、二进制、缺源码等）才为部分审查；说明已覆盖内容。未访问 diff 之外的仓库/调用方上下文不构成部分审查——此类盲点写入影响范围或未审查变更，并说明是否影响 verdict>
**变更规模：** <已审查文本 diff：文件数> 个文件, +<新增行数> 行 / -<删除行数> 行；<如有生成/二进制/锁/迁移文件在此单独说明，无则省略>
**风险等级：** <🔴 高 | 🟡 中 | 🟢 低> - <风险依据：变更类型 + 路径重要性 + 高风险领域 + 测试充分性>
**未审查变更：** <无 | 逐项列出：对象 + 未审查原因 + 可能隐藏的风险 + 是否影响 verdict>

## 执行摘要

<2-4 句话。说明变更意图、主要影响区域、是否发现阻断风险、提交前最重要的验证动作。不要重复下面所有细节。>

## 重点发现

1. [🔒|❌|⚠️|🧪|👁️|📈|🧭] <file:line-line | file:function | file:配置键 | file | 截图/页面区域 | 多文件契约> - <可操作问题标题：错误机制 + 受影响对象 + 触发条件；避免“潜在问题”“需要注意”等空泛标题>
   - 证据：<最短直接证据：diff/上下文/schema/测试/截图；必须支撑该发现，不复制大段代码>
   - 影响：<受影响对象 + 触发条件 + 失败模式 + 最坏后果；说明是否用户可见、数据相关或生产相关>
   - 修复：<代码级修改方向 + 边界条件 + 首选方案>
   - 验证：<最小验证闭环：具体命令/测试名/手动路径 + 覆盖场景 + 预期结果>
   - 置信度：<高 | 中 | 低> - <证据强度说明：直接由 diff 证明 / 需要调用方上下文 / 需要运行或产品确认>
   - 阻塞原因：<为什么这会阻塞提交；仅阻塞项包含此行>

仅在置信度非高时追加 `   - 置信度：<中 | 低> - <一行原因>`；高置信度的发现省略此行，使该字段表达不确定性而非例行填充。
仅阻塞项追加 `   - 阻塞原因：<为什么这会阻塞提交；仅阻塞项包含此行>`。如果没有重点发现，写：

无。

## 提交建议

- **提交前必须处理：** <只列阻塞项；没有则写 `无`>
- **提交前建议处理：** <非阻塞但建议本次一起处理的问题；没有则写 `无`>
- **可后续跟进：** <不影响本次提交的维护性、文档、清理事项；没有则写 `无`>
- **建议验证：** <按优先级列出最小验证闭环：命令/测试名/手动路径 + 覆盖场景 + 预期结果；如无法运行，说明替代验证>
- **建议补充说明：** <PR 描述、变更原因、回滚说明、迁移说明、截图、监控说明；没有则写 `无`>

## 变更概览

- **修改：** <按责任域总结行为变化：业务逻辑/API/UI/配置/测试/文档/依赖，不要只列文件名>
- **新增：** <新增能力、入口、测试、配置、迁移、依赖及用途；没有则写 `无 - 仅修改。`>
- **删除：** <删除的行为、兼容层、配置、测试或资源，并说明是否有替代路径；没有则写 `无。`>
- **行为变化：** <无用户/API/数据/权限/性能/发布行为变化 | 明确说明变化前、变化后、受影响对象和触发条件>

## 风险摘要

| 维度 | 结论 | 依据 |
|---|---|---|
| 正确性 | <通过 / 有风险 / 未充分验证> | <简短依据> |
| 安全与隐私 | <无明显风险 / 有风险 / 未充分验证> | <认证、授权、凭证、PII、注入、供应链等> |
| 数据与迁移 | <不涉及 / 有风险 / 未充分验证> | <schema、迁移、兼容、回滚、数据完整性等> |
| 性能与扩展性 | <无明显风险 / 有风险 / 未充分验证> | <查询、循环、缓存、批量、资源占用等> |
| 兼容性 | <无破坏 / 有风险 / 未充分验证> | <API、配置、序列化、前后端、版本兼容等> |
| 可观测性与回滚 | <充足 / 不足 / 不适用> | <日志、指标、feature flag、rollback plan 等> |
| 测试覆盖 | <充足 / 有缺口 / 未运行> | <单测、集成、E2E、手动验证、快照等> |

仅存在具体提交后监控事项时追加 `- **监控点：** <具体日志、指标、仪表盘、错误或用户行为>`。

## 影响范围

- **直接影响：** <变更直接触达的模块、入口、用户路径、服务或任务>
- **间接影响：** <可能受影响 of 调用方、依赖、消费者、数据管道、部署流程；无则写 `无明显间接影响。`>
- **需要领域确认：** <无 | 领域 + 需要确认的问题 + 阻塞/非阻塞>

## 回归风险

**等级：** <🔴 高 | 🟡 中 | 🟢 低>
**原因：** <具体说明为什么是该等级>
**最小验证闭环：** <最少需要哪些测试或手动步骤才能降低风险>
```

#### Chinese Tiny Diff Review

```markdown
# 提交前审查

**VERDICT:** <SAFE_TO_COMMIT | SAFE_TO_COMMIT_WITH_NOTES | DO_NOT_COMMIT>
**结论：** <简短、可执行的 1-2 句决策摘要：是否可提交 + 主要依据/最高风险 + 提交前必须动作或建议验证>
**差异来源：** <来源 + 精确范围：命令/PR/commit range/用户粘贴 diff；说明 staged/unstaged/基准分支>
**审查范围：** <若 diff 的每个 hunk 都已审查则为完整审查；仅当 diff 本身有无法审查的部分（截断、二进制、缺源码等）才为部分审查；说明已覆盖内容。未访问 diff 之外的仓库/调用方上下文不构成部分审查——此类盲点写入影响范围或未审查变更，并说明是否影响 verdict>
**变更规模：** <已审查文本 diff 规模；单独说明生成文件/二进制/锁文件/迁移文件>
**Tiny Diff 适用性：** <适用 | 不适用但已降级摘要 | 不适用，应使用默认模板>

- **变更：** <具体风险依据：变更范围 + 高风险领域 + 测试/审查充分性>
- **逻辑：** <无运行时行为变化，并说明依据 | 有变化：变化前/变化后/触发条件/受影响对象>
- **影响范围：** <直接影响 + 间接影响 + 已确认不受影响的边界；避免只写“自包含”，除非说明依据>
- **风险：** <🔴 高 | 🟡 中 | 🟢 低> - <风险依据：变更类型 + 路径重要性 + 高风险领域 + 测试充分性>
- **建议验证：** <已覆盖/未覆盖 + 最小建议验证 + 是否影响提交决策>
- **提交前：** <无 | 必须处理的阻塞项>
```

---

## Visual Review Skeleton

### Chinese Visual Review Skeleton

```markdown
# 提交前审查

> ⚠️ **部分审查:** <仅在无法完整审查视觉/交互/生成物时出现；说明原因>. 未审查部分列于“未审查变更”。

**VERDICT:** <SAFE_TO_COMMIT | SAFE_TO_COMMIT_WITH_NOTES | DO_NOT_COMMIT>
**结论：** <简短、可执行的 1-2 句决策摘要：是否可提交 + 主要依据/最高风险 + 提交前必须动作或建议验证>
**差异来源：** <来源 + 精确范围：命令/PR/commit range/用户粘贴 diff；说明 staged/unstaged/基准分支>
**审查范围：** <若 diff 的每个 hunk 都已审查则为完整审查；仅当 diff 本身有无法审查的部分（截断、二进制、缺源码等）才为部分审查；说明已覆盖内容。未访问 diff 之外的仓库/调用方上下文不构成部分审查——此类盲点写入影响范围或未审查变更，并说明是否影响 verdict>
**变更规模：** <已审查文本 diff 规模；单独说明生成文件/二进制/锁文件/迁移文件>
**视觉范围：** <已审查页面/组件 + 状态 + viewport + 主题 + 语言 + 浏览器/平台；未覆盖项必须写入未审查变更>
**未审查变更：** <无 | 逐项列出：对象 + 未审查原因 + 可能隐藏的风险 + 是否影响 verdict>

## 视觉审查矩阵

| 领域 | 信号 | 依据 | 结论 |
|---|---|---|---|
| 代码质量与设计一致性 | <通过/问题/未验证> | <设计系统 token、间距、颜色、组件规范> | <影响与动作> |
| 布局与响应式 | <通过/问题/未验证> | <截图区域、组件、viewport、diff 依据> | <影响与动作> |
| 交互状态 | <通过/问题/未验证> | <hover/focus/disabled/loading/error/empty state 等> | <影响与动作> |
| 可访问性 | <通过/问题/未验证> | <语义、键盘、焦点、ARIA、对比度、屏幕阅读器影响> | <影响与动作> |
| 文案与本地化 | <通过/问题/未验证> | <文案、换行、长度、中文/英文/RTL 等> | <影响与动作> |
| 回归风险 | <低/中/高> | <受影响页面、共享组件、快照变化> | <验证建议> |

## 重点发现

1. [🔒|❌|⚠️|🧪|👁️|📈|🧭] <file:line-line | file:function | file:配置键 | file | 截图/页面区域 | 多文件契约> - <可操作问题标题：错误机制 + 受影响对象 + 触发条件；避免“潜在问题”“需要注意”等空泛标题>
   - 证据：<最短直接证据：diff/上下文/schema/测试/截图；必须支撑该发现，不复制大段代码>
   - 影响：<受影响对象 + 触发条件 + 失败模式 + 最坏后果；说明是否用户可见、数据隔离或品牌一致性问题>
   - 修复：<具体 UI、样式、组件、文案或状态处理建议>
   - 验证：<最小验证闭环：具体命令/测试名/手动路径 + 覆盖场景 + 预期结果>
   - 置信度：<高 | 中 | 低> - <证据强度说明：直接由 diff 证明 / 需要截图/运行验证>
   - 阻塞原因：<为什么这会阻塞提交；仅阻塞项包含>

如果没有重点发现，写：

无。

## 提交建议

- **提交前必须处理：** <阻塞项或 `无`>
- **建议验证：** <截图路径、viewport、主题、浏览器、交互状态、可访问性检查>
- **建议补充材料：** <PR 截图、录屏、设计稿链接、Storybook 链接、测试说明；没有则写 `无`>

## 风险细节

- **用户可见变化：** <无 | 简述>
- **受影响路径：** <页面、组件、入口、角色、设备>
- **未覆盖状态：** <无 | loading/error/empty/disabled/focus/mobile/dark mode/i18n 等>
- **回归风险：** <🔴 高 | 🟡 中 | 🟢 低> - <原因>
```

---

## Review Limitations / 审查限制

```markdown
- [👁️] <限制标题>
  - 范围：<哪些文件、资源、状态、上下文没有被审查>
  - 原因：<过大、二进制、生成文件、缺少截图、缺少依赖、无法运行、上下文不足等>
  - 风险：<该限制可能隐藏什么问题>
  - 建议：<补充 diff、截图、测试结果、CI 日志、设计稿、schema、调用方上下文等>
  - 是否阻塞：<是 | 否> - <原因>
```

## Machine-Readable Metadata / 机器可读摘要块

```markdown
<!-- review-meta
verdict: <SAFE_TO_COMMIT | SAFE_TO_COMMIT_WITH_NOTES | DO_NOT_COMMIT>
blockers: <number>
warnings: <number>
test_gaps: <number>
review_limits: <number>
risk: <high | medium | low>
scope: <full | partial>
template: <default | tiny | visual>
-->
```

---

## 极简示范样本 / Minimal Few-Shot Examples

本节包含中文 Default 和 Tiny 模式的极简填充样例，用作模型输出格式的静态锚点。日常 Default/Tiny 审查应当参考此样例的排版与对齐逻辑进行生成。

### 示范 1：中文日常评审 (Default Developer Review) 填充样例

```markdown
# 提交前审查

**VERDICT:** SAFE_TO_COMMIT_WITH_NOTES
**结论：** 可以提交添加语言列的迁移，但建议在本次提交中一并编写 `getLocale` 方法的单元测试。
**统计：** 0 个阻塞项 · 1 个非阻塞提醒 · 1 个测试缺口 · 0 个审查限制
**差异来源：** 暂存区差异，经 helper 脚本（`scripts/collect_diff_context.sh`）提取
**审查范围：** 完整审查 - 已审查 `schema.prisma` 与 `userRepo.ts` 的全部 hunk；未访问数据库不构成部分审查
**变更规模：** 2 个文件, +24 行 / -3 行
**风险等级：** 🟡 中 - schema 变更触及数据完整性维度，但为附加列且有默认值
**未审查变更：** 无

## 执行摘要

本次变更为 `users` 表新增 `preferred_locale` 可空列（默认 'en-US'）。未发现阻塞性风险。存量数据行将采用默认值填充，主风险是未来查询时的默认值回退一致性，建议补充测试后提交。

## 重点发现

1. ⚠️ `src/repo/userRepo.ts:22` - 未为新增的 `getLocale` 方法编写单元测试
   - 证据：diff 修改了数据库读取，但测试目录下无对应用例更改
   - 影响：未来如果默认值回退逻辑变更，缺少自动化回归防护网
   - 修复：在 `userRepo.test.ts` 中补充覆盖 `getLocale` 返回 NULL 与正确语言的测试
   - 验证：运行 `pnpm test userRepo` 全绿且覆盖率不降低
   - 置信度：高

## 提交建议

- **提交前必须处理：** 无
- **提交前建议处理：** 补充 `getLocale` 单元测试
- **可后续跟进：** 无
- **建议验证：** `pnpm test userRepo` 验证查询逻辑
- **建议补充说明：** PR 描述中附带迁移回滚的 down 脚本

## 变更概览

- **修改：** 数据访问 - `userRepo` 增加 `getLocale` 获取逻辑
- **新增：** Prisma 字段 `preferred_locale`
- **删除：** 无
- **行为变化：** 查询用户语言时若无设置，回退返回默认值 'en-US'

## 风险摘要

| 维度 | 结论 | 依据 |
|---|---|---|
| 正确性 | 通过 | 附加字段无逻辑冲突 |
| 安全与隐私 | 无明显风险 | 无敏感数据暴露 |
| 数据与迁移 | 有风险 | 大表上执行 DDL 迁移可能产生短暂锁，需确保 PG 版本 ≥11 |
| 性能与扩展性 | 通过 | 单行主键查询，无性能风险 |
| 兼容性 | 无破坏 | 附加列，向后兼容 |
| 可观测性与回滚 | 充足 | 迁移自带 down 脚本，可快速回滚 |
| 测试覆盖 | 有缺口 | 读取逻辑缺乏单测覆盖 |

## 影响范围

- **直接影响：** `userRepo` 读取和 `users` 数据库表
- **间接影响：** 无明显间接影响
- **需要领域确认：** 无

## 回归风险

**等级：** 🟡 中
**原因：** 涉及数据库 schema 变更，但属添加列且有回滚 down 脚本
**最小验证闭环：** staging 库跑一次 migration 并 rollback 成功

<!-- review-meta
verdict: SAFE_TO_COMMIT_WITH_NOTES
blockers: 0
warnings: 1
test_gaps: 1
review_limits: 0
risk: medium
scope: full
template: default
-->
```

### 示范 2：中文极简评审 (Tiny Diff Review) 填充样例

```markdown
# 提交前审查

**VERDICT:** SAFE_TO_COMMIT
**结论：** 可以提交；这是 README 文案修正，无运行时风险。
**差异来源：** 暂存区差异，基于 `git diff --cached` 提取
**审查范围：** 完整审查 - 包含 README.md 的单一 hunk
**变更规模：** 1 个文件, +2 行 / -2 行
**Tiny Diff 适用性：** 适用

- **变更：** 修正 README 中的命令拼写错误
- **逻辑：** 无运行时行为变化
- **影响范围：** 仅影响文档阅读者
- **风险：** 🟢 低 - 文档变更不改变构建链与代码
- **测试：** 无需额外测试
- **提交前：** 无

<!-- review-meta
verdict: SAFE_TO_COMMIT
blockers: 0
warnings: 0
test_gaps: 0
review_limits: 0
risk: low
scope: full
template: tiny
-->
```
